---
apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
  labels:
    app: "{{ .Release.Name }}"
spec:
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres-standard
  type: NodePort
  selector:
    app: "{{ .Release.Name }}"
